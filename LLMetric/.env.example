# LLMetric environment example

# Choose ONE mode below. Keep only one section uncommented.

# ----- Local Supabase (Docker) -----
# When using local Supabase stack (e.g. supabase/start via Docker), enable local mode.
# In local mode, app uses http://127.0.0.1:54321 and built-in anon/service keys.
#SUPABASE_USE_LOCAL=true

# ----- Remote Supabase (Dashboard) -----
# For production or remote development, disable local mode and provide all keys.
#SUPABASE_USE_LOCAL=false
#NEXT_PUBLIC_SUPABASE_URL=
#NEXT_PUBLIC_SUPABASE_ANON_KEY=
#SUPABASE_SERVICE_ROLE_KEY=

# Notes:
# - .env.local must NOT be committed. This example is the only tracked template.
# - validateSupabaseEnv() logs the active mode in development builds.
